import { PlanGenerationWizard } from '@/components/plan/PlanGenerationWizard';

export default function PlanGeneratePage() {
    return (
        <main className="min-h-screen bg-slate-950">
            <PlanGenerationWizard />
        </main>
    );
}
